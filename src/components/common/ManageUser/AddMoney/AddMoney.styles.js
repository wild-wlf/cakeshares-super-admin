import styled from 'styled-components';

export const StyledAddMoney = styled.div`
  .title {
    display: block;
    margin-bottom: 25px;
    font-size: 18px;
    font-weight: 400;
    line-height: 14px;
  }
  .balance {
    font-weight: 600;
    color: var(--secondary-50);
    padding-left: 20px;
  }
  .balanceWrap {
    display: flex;
  }
`;
